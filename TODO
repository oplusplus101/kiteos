osutil      [×]
types       [×]
driver      [×]
keyboard    [×]
mouse       [×]
interrupts  [×]
port        [×]
gdt         [×]
kernel      [×]

make pci to support base address registers [×]
add vga support [×]
make a basic gui [ ]